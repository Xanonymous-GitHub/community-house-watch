[project]
name = "community_house_watch"
version = "0.1.0"
description = "A dog that help you watch 591 house change in specific communities"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "crawlee[all]>=0",
]

[dependency-groups]
dev = [
    "ruff>=0",
]
